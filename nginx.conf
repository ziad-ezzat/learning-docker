events {}

http{
    server{
        listen 80;
        root /usr/share/nginx/html;
        location / {
            index project.html;
        }
        location /usr/share/nginx/html {
               autoindex on;
        }
    }
}

